<!-- ================================================================
  🤖 AI SESSION CONTEXT — 給下一個 AI Session 看的專案記憶
  最後更新：2026-06-12，Session 05a0c700
  ================================================================ -->

## 🤖 AI 快速喚醒區（給 Copilot / AI 看）
> 下次回到此專案，請先讀本節，再閱讀其他說明文件，即可還原完整開發背景。

### 專案定位
本專案為「PP00 竹北借機系統」（PP00 Tester Booking System），提供使用者以網頁形式透過平面圖或卡片模式預約測試機台。

### 重要技術決策
| 項目 | 說明 |
| --- | --- |
| 前端架構 | Vanilla HTML / CSS / JS，由 `static/js/app.js` 與 `static/js/config.js` 定義機台座標與設定值 |
| 資料庫 | 連接 Supabase 進行預約資料儲存與即時更新 |
| 機台排法異動 (2026-06-12) | 1. `Ms3480#1` 與右側 `UF3000` 交換位置，且該 `UF3000` 更名為「點針座1」<br>2. `Ms3490#3` 往右移至 `x:27.8, y:25.5`，原位置新增「點針座2」設備<br>3. 第三排整排往上移到 `y:48.5`；第四排整排往下移到 `y:71.5`<br>4. 原 `y:48.5` 與 `71.5` 走道字樣拿掉，並在 `y:55.5` 與 `63.5` 新增走道<br>5. 第四排最右側 `UF3000` 往左靠至 `x:45.0`<br>6. 第三排 `T5830ES_WBN11(.78)` 左移至 `x:27.8`，且在其右側新增一台 `UF3000` 設備位於 `x:53.6` |

### 尚未完成的功能
- [ ] 進一步的介面微調與功能擴充。
